'use strict'

const fs          = require('fs');
const path        = require('path');
const querystring = require( 'querystring' );
const url         = require( 'url' );

module.exports = (function(){

  var renderFiles = function( request, respond ) {

  };
  return renderFiles;
}());